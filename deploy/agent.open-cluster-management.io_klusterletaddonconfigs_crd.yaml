# Copyright Contributors to the Open Cluster Management project

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.5.0
  creationTimestamp: null
  name: klusterletaddonconfigs.agent.open-cluster-management.io
spec:
  group: agent.open-cluster-management.io
  names:
    kind: KlusterletAddonConfig
    listKind: KlusterletAddonConfigList
    plural: klusterletaddonconfigs
    singular: klusterletaddonconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: KlusterletAddonConfig is the Schema for the klusterletaddonconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KlusterletAddonConfigSpec defines the desired state of KlusterletAddonConfig
            properties:
              applicationManager:
                description: KlusterletAddonAgentConfigSpec defines configuration for each addon agent.
                properties:
                  enableGlobalProxy:
                    description: EnableGlobalProxy is the flag to enable/disable the GlobalProxy configuration for the pods of addon. default is false.
                    enum:
                    - true
                    - false
                    type: string
                  enabled:
                    description: Enabled is the flag to enable/disable the addon. default is false.
                    type: boolean
                type: object
              certPolicyController:
                description: KlusterletAddonAgentConfigSpec defines configuration for each addon agent.
                properties:
                  enableGlobalProxy:
                    description: EnableGlobalProxy is the flag to enable/disable the GlobalProxy configuration for the pods of addon. default is false.
                    enum:
                    - true
                    - false
                    type: string
                  enabled:
                    description: Enabled is the flag to enable/disable the addon. default is false.
                    type: boolean
                type: object
              clusterLabels:
                additionalProperties:
                  type: string
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                type: object
              clusterName:
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                minLength: 1
                type: string
              clusterNamespace:
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                minLength: 1
                type: string
              globalProxy:
                description: GlobalProxy defines the cluster-wide proxy configuration of managed cluster.
                properties:
                  httpProxy:
                    description: HTTPProxy is the URL of the proxy for HTTP requests.  Empty means unset and will not result in an env var.
                    type: string
                  httpsProxy:
                    description: HTTPSProxy is the URL of the proxy for HTTPS requests.  Empty means unset and will not result in an env var.
                    type: string
                  noProxy:
                    description: NoProxy is a comma-separated list of hostnames and/or CIDRs for which the proxy should not be used. Empty means unset and will not result in an env var.
                    type: string
                type: object
              iamPolicyController:
                description: KlusterletAddonAgentConfigSpec defines configuration for each addon agent.
                properties:
                  enableGlobalProxy:
                    description: EnableGlobalProxy is the flag to enable/disable the GlobalProxy configuration for the pods of addon. default is false.
                    enum:
                    - true
                    - false
                    type: string
                  enabled:
                    description: Enabled is the flag to enable/disable the addon. default is false.
                    type: boolean
                type: object
              imagePullPolicy:
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                enum:
                - Always
                - Never
                - IfNotPresent
                type: string
              imagePullSecret:
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                minLength: 1
                type: string
              imageRegistry:
                description: ImageRegistry defined the custom registry address of the images. DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector defines which Nodes the Pods are scheduled on. The default is an empty list. DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                type: object
              policyController:
                description: KlusterletAddonAgentConfigSpec defines configuration for each addon agent.
                properties:
                  enableGlobalProxy:
                    description: EnableGlobalProxy is the flag to enable/disable the GlobalProxy configuration for the pods of addon. default is false.
                    enum:
                    - true
                    - false
                    type: string
                  enabled:
                    description: Enabled is the flag to enable/disable the addon. default is false.
                    type: boolean
                type: object
              searchCollector:
                description: KlusterletAddonAgentConfigSpec defines configuration for each addon agent.
                properties:
                  enableGlobalProxy:
                    description: EnableGlobalProxy is the flag to enable/disable the GlobalProxy configuration for the pods of addon. default is false.
                    enum:
                    - true
                    - false
                    type: string
                  enabled:
                    description: Enabled is the flag to enable/disable the addon. default is false.
                    type: boolean
                type: object
              version:
                description: DEPRECATED in release 2.4 and will be removed in release 2.5 since not used anymore.
                type: string
            required:
            - applicationManager
            - certPolicyController
            - iamPolicyController
            - policyController
            - searchCollector
            type: object
          status:
            description: KlusterletAddonConfigStatus defines the observed state of KlusterletAddonConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
