apiVersion: apps/v1
kind: Deployment
metadata:
  name: ibm-klusterlet-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: ibm-klusterlet-operator
  template:
    metadata:
      labels:
        name: ibm-klusterlet-operator
    spec:
      serviceAccountName: ibm-klusterlet-operator
      containers:
        - name: ibm-klusterlet-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - ibm-klusterlet-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "ibm-klusterlet-operator"
